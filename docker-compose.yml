name: judilibre-admin

services:
  judilibre-admin:
    env_file:
      - .env
    ports:
      - ${API_PORT}:${API_PORT}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./judilibre-admin:/home/node
